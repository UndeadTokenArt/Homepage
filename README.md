# Instructions to build and run the Docker container

## Build the Docker image
```docker build -t undeadtoken004:latest .```

## Run the Docker container
```docker run -p 8080:8080 undeadtoken004:latest```